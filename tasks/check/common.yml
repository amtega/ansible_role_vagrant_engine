---
# Role check tasks

- block:
    - name: assert platform is supported
      assert:
        that:
          - distribution in vagrant_engine_supported_distributions
          - >-
            ansible_facts.distribution_version
            is version(vagrant_engine_supported_versions[distribution], ">=")
      vars:
        distribution: "{{ ansible_facts.distribution | lower }}"
  tags:
    - role::vagrant_engine
    - role::vagrant_engine::check
