---
# Tasks for tesing role

- name: test vagrant_engine the role
  hosts: localhost
  tasks:
    - include_role:
        name: vagrant_engine
      vars:
        vagrant_engine_users:
          - "{{ ansible_user_id }}"
